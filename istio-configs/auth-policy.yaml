apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: product-access-control
  namespace: dev
spec:
  selector:
    matchLabels:
      app: product
  action: ALLOW
  rules:
  - from:
    - source:
        namespaces: ["dev"]